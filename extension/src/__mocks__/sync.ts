import { jest } from '@jest/globals';

export const WindowTracker = jest.fn();
export const SyncManager = jest.fn();
export const TabEventHandler = jest.fn();
export const UserSettingsManager = jest.fn();
export const MessageHandler = jest.fn();